# Core Configuration
# These are the only required variables for 10_core layer
aws_region   = "ap-southeast-1"
project_name = "tt-practice"
environment  = "dev"

# Note: All other configuration (log retention, KMS, secrets) is managed
# by sub-modules using their defaults or removed from practice project.
