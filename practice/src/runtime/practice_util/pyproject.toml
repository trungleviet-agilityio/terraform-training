[project]
name = "practice-util"
version = "0.1.0"
description = "Shared utility functions for practice Lambda functions"
requires-python = ">=3.13"

dependencies = [
    "boto3>=1.34.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
