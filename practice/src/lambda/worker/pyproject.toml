[project]
name = "worker"
version = "0.1.0"
description = "Worker Lambda function for SQS message processing"
requires-python = ">=3.13"

dependencies = [
    "practice-util",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
